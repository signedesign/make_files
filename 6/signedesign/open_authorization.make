; Authorization
; authorization.make
; Authorizes access.

; Include requirements
  ; includes[] = ""

; OAuth 
; OAuth implements the OAuth standard for use with Drupal
  projects[oauth][subdir] = "contrib/signedesign"
  projects[oauth][version] = "3.0-beta3"
  ; works with Services 3.x 
 