; configuration_requirements
; Install requirements for configuration handling.

; Ctools
  projects[ctools][subdir] = "contrib/signedesign"
  projects[ctools][version] = "1.8"
  ; required by Strongarm
  ; included by configuration.make
    