; People
; people.make


; Content Profile
  ; Content Profile provides user profiles to Drupal as nodes.
  projects[content_profile][subdir] = "contrib"
  projects[content_profile][version] = "1.0"

; ImageCache Profiles
  ; omschrijving ontbreekt
  projects[imagecache_profiles][subdir] = "contrib"
  projects[imagecache_profiles][version] = "1.3"

; Smartqueue per User
  ; Smartque Per User allows users to order nodes as they wish.
  projects[smartqueue_users][subdir] = "contrib"
  projects[smartqueue_users][version] = "1.x-dev"
  ; requires nodequeue (see interface.make)
